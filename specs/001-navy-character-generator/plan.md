# Implementation Plan: Navy Character Generator

**Branch**: `001-navy-character-generator` | **Date**: 2026-06-17 | **Spec**: [spec.md](spec.md)
**Rules Sources**:
- Career tables: https://evolvedexperiment.github.io/cepheus-srd/character-creation.html#career-tables
- Pseudo-hex notation (canonical): https://evolvedexperiment.github.io/cepheus-srd/introduction.html#pseudo-hexadecimal-notation
- Canonical terminology: https://evolvedexperiment.github.io/cepheus-srd/introduction.html#important-terms

**Input**: Feature specification from `/specs/001-navy-character-generator/spec.md`

## Summary

Build a Cepheus Engine Navy character generator as a data-driven Python library, exposed via a `cetools character generate` Typer CLI subcommand. The core engine accepts a `Career` frozen dataclass and a `DiceRoller` protocol, enabling deterministic testing and future extensibility to additional careers without engine modification.

## Technical Context

**Language/Version**: Python 3.13

**Primary Dependencies**: Typer ≥ 0.15 (CLI), pytest ≥ 8 (tests), Black ≥ 24 (formatter), flake8 ≥ 7 (linter); no runtime dependencies beyond Typer

**Storage**: N/A — pure computation, no persistence

**Testing**: pytest with RNG injection for deterministic character generation tests

**Target Platform**: macOS/Linux CLI (Python package via `uv`)

**Project Type**: CLI tool with decoupled library core

**Performance Goals**: < 2 seconds per invocation (SC-001); pure computation, trivially achievable

**Constraints**: No I/O within the engine; CLI is the only delivery layer in MVP; no user-configurable flags in MVP invocation

**Scale/Scope**: Single character per invocation; one career (Navy) in MVP

## Constitution Check

*Constitution v1.0.0 ratified 2026-06-17. All five principles apply.*

- **I — SRD-Fidelity**: The spec FRs contained incorrect target numbers and rank titles. Implementation follows the SRD throughout; all corrections are documented in [research.md](research.md#discrepancies-spec-vs-srd). No deliberate deviations from SRD rules exist in this feature — MET.
- **II — Library-First**: The generation engine (`src/cetools/engine/`) has zero dependency on Typer or any delivery layer. FR-013 enforces this at the module boundary — MET by design.
- **III — CLI Interface**: `cetools character generate`; stdout for success (exit 0), stderr for failure (exit 1), no game logic in CLI code (FR-015, FR-016) — MET by design.
- **IV — Test-First**: All phases write failing tests before implementation; pytest suite must be green before any PR — REQUIRED.
- **V — Data-Driven Extensibility**: All career variation lives in a `Career` frozen dataclass (FR-014). The engine imports `Career` and `DiceRoller` only — zero Navy-specific references permitted in `generator.py` — MET by frozen `Career` dataclass pattern.

No gate violations.

## Project Structure

### Documentation (this feature)

```text
specs/001-navy-character-generator/
├── plan.md              # This file
├── research.md          # Phase 0 — rules research and discrepancy log
├── data-model.md        # Phase 1 — entities, Career interface, generation state machine
├── quickstart.md        # Phase 1 — validation scenarios
├── contracts/
│   └── cli.md           # CLI command contract (exit codes, stdout/stderr, library API)
└── tasks.md             # Phase 2 — generated by /speckit-tasks (not yet created)
```

### Source Code

```text
src/cetools/
├── __init__.py
├── cli/
│   ├── __init__.py
│   ├── main.py              # Root Typer app; adds character sub-app
│   └── character.py         # `character` group; `generate` command
├── engine/
│   ├── __init__.py
│   ├── careers/
│   │   ├── __init__.py
│   │   ├── base.py          # Career + RankEntry frozen dataclasses
│   │   └── navy.py          # NAVY_CAREER instance
│   ├── dice.py              # DiceRoller protocol + default implementation
│   ├── pseudohex.py         # encode/decode/encode_upp functions
│   ├── models.py            # Character, Skill, Benefit, Term, GenerationFailure
│   └── generator.py         # generate_character() — pure engine, no I/O
└── formatter.py             # Plain-text output formatter

tests/
├── test_pseudohex.py        # All 34 mappings, boundary values, invalid inputs
├── test_models.py           # UPP encoding, characteristic modifiers
├── test_careers.py          # Navy career data structure integrity
├── test_generator.py        # Full lifecycle: enlistment, terms, aging, mustering-out, pension
├── test_formatter.py        # Output contains all required labeled sections
└── test_cli.py              # Exit codes, stdout/stderr routing
```

**Structure Decision**: Single project layout (Option 1). No web frontend, no multi-package monorepo needed for MVP. The `cli/` and `engine/` split enforces FR-013 (library independence) at the module boundary.

## Key Design Decisions

### Career Data Interface

All career variation lives in a `Career` frozen dataclass (see [data-model.md](data-model.md)). The generation engine imports `Career` and `DiceRoller` only — no `NavyCareer` references. Adding a new career requires: define a `Career` instance in `careers/<name>.py`, wire it to a CLI command. Zero engine changes.

### RNG Injection

`generate_character(career, roller=DiceRoller())` accepts an injectable roller. Default uses `random.randint`. Tests inject a controlled roller to produce deterministic outcomes for every branch of the generation state machine.

### SRD Corrections

The spec FRs contain values that differ from the SRD. **Implementation follows SRD** (per spec Assumptions). See [research.md](research.md#discrepancies-spec-vs-srd) for the full correction table. Key differences:
- Qualification: Intelligence 6+ (not Endurance 5+)
- Commission: Social Standing 7+ (not Education 6+)
- Advancement: Education 6+ (not Education 7+)
- Rank titles: Starman–Commodore (not Ensign–Fleet Admiral)
- Rank 3 bonus: Tactics-1 (not Leadership-1)

### Background Skills (Simplified)

Per spec Assumption: 3 skills at Level 0 from a default list (not Education-DM-scaled, not homeworld-dependent). Full homeworld generation is deferred.

### Aging Distribution

When aging reduces multiple physical stats, the player distributes reductions. In a CLI-only MVP with no player input mid-generation, reductions are applied in order: Strength first, Dexterity second, Endurance third (deterministic; no user prompt required).

## Complexity Tracking

No constitution violations. No complexity justification needed.
